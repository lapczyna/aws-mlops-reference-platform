[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "batch-inference-platform"
version = "0.1.0"
description = "Serverless batch inference platform reference architecture on AWS."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "Artur Lapczynski" }]
classifiers = [
    "Programming Language :: Python :: 3.12",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Private :: Do Not Upload",
]

# Runtime dependencies for Lambda handlers and the shared application package.
# Kept in lockstep with src/requirements.txt, which is what actually gets
# packaged into the Lambda Layer (see ADR-0007) -- boto3 is the one
# exception, provided by the Lambda runtime itself.
dependencies = [
    "boto3>=1.34,<2",
    "aws-lambda-powertools>=2.38,<3",
    "pydantic>=2.6,<3",
    "python-ulid>=2,<4",
]

[project.optional-dependencies]
dev = [
    "ruff>=0.4,<1",
    "black>=24.3,<25",
    "mypy>=1.9,<2",
    "boto3-stubs[essential,s3,dynamodb,stepfunctions,sagemaker,sagemaker-runtime]>=1.34,<2",
    "pytest>=8.1,<9",
    "pytest-cov>=5.0,<6",
    "pytest-mock>=3.14,<4",
    "moto[s3,dynamodb,stepfunctions,sagemaker]>=5.0,<6",
    "pre-commit>=3.7,<4",
]
# Local-only tooling for training and packaging the reference model (ml/).
# Deliberately NOT in `dependencies` or src/requirements.txt: these are
# heavy binary wheels irrelevant to the Lambda functions, and including them
# in the Lambda Layer would bloat it for no runtime benefit.
ml = [
    "scikit-learn>=1.4,<2",
    "pandas>=2.2,<3",
    "joblib>=1.3,<2",
]

[tool.setuptools.packages.find]
where = ["src"]

# ---------------------------------------------------------------------------
# Ruff -- linting
# ---------------------------------------------------------------------------
[tool.ruff]
line-length = 100
target-version = "py312"
src = ["src", "tests"]

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort
    "N",    # pep8-naming
    "UP",   # pyupgrade
    "B",    # flake8-bugbear
    "C4",   # flake8-comprehensions
    "SIM",  # flake8-simplify
    "S",    # flake8-bandit (security)
    "A",    # flake8-builtins
    "T20",  # flake8-print (no stray print/debug statements)
    "PT",   # flake8-pytest-style
    "RUF",  # ruff-specific rules
]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["S101", "S105", "S106"]
# ml/ scripts are standalone CLI tools (training, packaging), not Lambda
# handlers -- print() is the correct way to report progress to a user
# running them interactively.
"ml/**" = ["T201"]

[tool.ruff.lint.isort]
known-first-party = ["batch_inference_platform"]

# ---------------------------------------------------------------------------
# Black -- formatting
# ---------------------------------------------------------------------------
[tool.black]
line-length = 100
target-version = ["py312"]
include = '\.pyi?$'

# ---------------------------------------------------------------------------
# mypy -- static typing (strict mode)
# ---------------------------------------------------------------------------
[tool.mypy]
python_version = "3.12"
mypy_path = "src"
strict = true
warn_unused_ignores = true
warn_return_any = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
no_implicit_optional = true
show_error_codes = true
plugins = ["pydantic.mypy"]

[[tool.mypy.overrides]]
module = "tests.*"
disallow_untyped_defs = false

# ---------------------------------------------------------------------------
# pytest
# ---------------------------------------------------------------------------
[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-ra --strict-markers --strict-config"
markers = [
    "unit: fast, isolated unit tests with no external I/O",
    "integration: tests exercising mocked AWS services (moto)",
]

[tool.coverage.run]
source = ["src/batch_inference_platform"]
branch = true

[tool.coverage.report]
fail_under = 85
show_missing = true
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
]
