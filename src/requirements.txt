# Runtime dependencies packaged into the shared Lambda Layer (see ADR-0007).
#
# boto3 / botocore are deliberately NOT listed here: the Lambda Python
# runtime already bundles them, and vendoring our own copy would only grow
# the layer for no behavioral benefit. If a feature ever needs a boto3
# version newer than the runtime's, pin it here explicitly at that point.
aws-lambda-powertools>=2.38.1,<3
pydantic>=2.6.4,<3
python-ulid>=2,<4
